// Video calling is temporarily disabled per product decision.
// Keeping a tiny placeholder export to avoid import errors if any stale imports remain.
export default function VideoCallDisabled() { return null; }


